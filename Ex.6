
#include <stdio.h>
#include <stdlib.h>


int isLower(char sym) {

  if(sym >= 'a' && sym <= 'z'){
    return 1;
  }else{
      return 0;
  }
}

int main() {
  if (isLower('a')){
    printf("Yes,it's lower-case letter");
  }
  else{
    printf("No,it's not a lower-case letter.");
  }
}
